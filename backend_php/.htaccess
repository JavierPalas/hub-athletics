RewriteEngine On
RewriteBase /api/

# Manejar peticiones OPTIONS (CORS preflight)
RewriteCond %{REQUEST_METHOD} OPTIONS
RewriteRule ^ - [L,R=200]

# Redirigir /api/leads a leads.php
RewriteRule ^leads/?$ leads.php [L]

# Redirigir raíz /api/ a index.php
RewriteRule ^/?$ index.php [L]
